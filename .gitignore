.DS_Store
.env
config.yaml
__pycache__/
*.pyc
.chats/
.feedback/
.state/

# The built index stays local until a distribution route is chosen
# (see "Distributing the index" in BUILD_PLAN.md / README)
index/
# transient dirs from refresh.sh (staging build) and fetch_index.sh (backup)
index.staging/
index.prev.*
index.bak.*

# Version-pinned shallow clones (rebuilt by src/checkouts.py)
checkouts/

.venv/
.pytest_cache/

# Build artifacts (index tarball for release upload)
dist/
