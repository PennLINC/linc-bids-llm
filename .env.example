# Copy to .env and fill in. NEVER commit .env (it is gitignored).

# --- Maintainer only (needed by ingest/probes; harvest-scope, read-only) ---
GITHUB_TOKEN=github_pat_...

# --- Members running the PoC need only this ---
OPENAI_API_KEY=sk-...
